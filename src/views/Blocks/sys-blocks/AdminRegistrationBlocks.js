import { Typography } from "@mui/material";
import { ControlledButton, ControlledTextField, ControlledCard, ControlledContainer } from "../../../components";

export const AdminRegistrationBlocks = () => {

    return (
        <ControlledContainer>
            <ControlledCard>
                <Typography variant='subtitle1'>Administrator Registration</Typography>
            </ControlledCard>
        </ControlledContainer>
    )
}